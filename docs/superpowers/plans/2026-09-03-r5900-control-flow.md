# R5900 Static Control-Flow Implementation Plan

**Goal:** Produce conservative basic blocks and explicit edges from ELF-backed executable R5900 code without executing guest instructions.

**Architecture:** Add `b3r_analysis`, depending on `b3r_runtime` and the R5900 decoder, with delay slots represented separately from the linear instruction list.

**Tech Stack:** C++20, CMake, CTest.

**Spec:** `docs/superpowers/specs/2026-09-03-r5900-control-flow-design.md`

### Task 1: Basic-block analyzer

**Files:**
- Create: `src/analysis/r5900_control_flow.h`
- Create: `src/analysis/r5900_control_flow.cpp`
- Create: `tests/r5900_control_flow_tests.cpp`
- Modify: `CMakeLists.txt`

- [x] Write synthetic guest-memory tests first.
- [x] Verify RED because the analysis API is absent.
- [x] Implement minimal basic-block/edge analysis.
- [x] Verify GREEN on GCC and the complete host suite.
- [x] Verify clean Release builds under GCC and Clang.
- [x] Publish to feature branch and verify Windows/MSVC CI (10/10 in run `33716632916`).
- [x] Update progress/validation docs with evidence.
