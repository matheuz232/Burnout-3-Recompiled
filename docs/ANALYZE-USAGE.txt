Burnout3Analyze - Windows x64
=============================

This package contains the static PS2 ELF analysis tool only.
It does not emulate or execute PS2 game code.

Basic analysis to a text report:

  .\Burnout3Analyze.exe --elf "D:\Games\Burnout3\SLUS_210.50" --output "burnout3-analysis.txt"

Write the report directly to the console:

  .\Burnout3Analyze.exe --elf "D:\Games\Burnout3\SLUS_210.50"

Increase the bounded reachable-CFG worklist and follow explicit direct calls:

  .\Burnout3Analyze.exe --elf "D:\Games\Burnout3\SLUS_210.50" --output "burnout3-analysis-with-callees.txt" --max-blocks 8192 --follow-direct-calls

Opt in to PS2 PAD binding discovery:

  .\Burnout3Analyze.exe --elf "D:\Games\Burnout3\SLUS_210.50" --output "burnout3-analysis-pad.txt" --max-blocks 8192 --follow-direct-calls --pad-bindings

--pad-bindings is analysis-only. Without it, the existing report is unchanged. With it, one PAD_BINDINGS_V0 section is appended.

Exact accepted PAD ELF symbols may resolve as Trusted when they point into file-backed executable PT_LOAD memory. Public-semantics static fingerprints are Candidate-only and never activate HLE. pc=none means there is no unique selected PC because the binding is unresolved or the static candidates are ambiguous.

No Burnout-specific guest address, proprietary byte signature or proprietary function hash is embedded in the scanner. The next milestone, PS2 PAD Runtime Confirmation v0, must confirm candidate PCs against live guest behavior before runtime binding is considered.

Direct call traversal is disabled by default. When enabled, explicit direct callees share the same --max-blocks budget and remain recorded as call evidence. Indirect JR/JALR destinations are never guessed, and following a direct call does not infer a function boundary.

Generated reports include UNKNOWN_PRIMARY_OPCODES, deterministic UNKNOWN_SITES and DIRECT_CALL_TARGETS. CALL_SITES is a static-reference count, not runtime frequency.

Show command-line help:

  .\Burnout3Analyze.exe --help

The region/executable filename may differ from the example above.
Use only an executable extracted from a legally obtained copy of the game.
Do not commit or distribute the game executable, assets, or other proprietary game data.
The generated analysis text report is the intended artifact to share back for evidence-driven decoder/recompiler work.
