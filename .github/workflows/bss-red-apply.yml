name: BSS RED Apply

on:
  push:
    branches:
      - feature/r5900-bss-clear-loop-v0
    paths:
      - .github/bss_red_apply.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply CMake RED registration
        run: python .github/bss_red_apply.py
