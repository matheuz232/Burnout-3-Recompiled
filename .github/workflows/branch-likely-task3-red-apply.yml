name: Branch Likely Task3 RED Apply

on:
  push:
    paths:
      - 'tools/apply_branch_likely_task3_red.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/r5900-branch-likely-v0
          fetch-depth: 0
      - name: Apply Task 3 RED
        run: python tools/apply_branch_likely_task3_red.py
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add CMakeLists.txt tests/r5900_x64_branch_likely_windows_tests.cpp
          git commit -m "test: add branch-likely x64 RED"
          git push origin HEAD:feature/r5900-branch-likely-v0
